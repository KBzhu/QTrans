# Changelog

All notable changes to this project will be documented in this file.

The format is based on Keep a Changelog.

## [Unreleased]

### Added
- **P10.8 传输通道管理**: 新增 `src/views/admin/ChannelManageView.vue`、`ChannelManageModal.vue`、`ChannelServerModal.vue` 与 `channel-manage.scss`，支持通道 CRUD、状态切换、服务器穿梭选择与拖拽排序；新增 `src/composables/useChannelManage.ts`、`src/api/channelManage.ts`、`src/mocks/handlers/channelManage.ts`、`src/types/channelManage.ts`；新增路由入口 `src/views/channel/index.vue` 并在 `src/router/routes.ts` 注册 `/channels`；更新 `api/index.ts`、`types/index.ts`、`mocks/handlers/index.ts`；新增 `useChannelManage.spec.ts`（12 个测试用例全通过）。
- **P10.7 区域管理**: 新增 `src/views/admin/RegionManageView.vue`（双 Tab：城市与安全域映射 / 可选安全域配置）、`src/views/admin/RegionManageModal.vue`（城市映射/安全域 共用弹窗，含表单校验与预设颜色选择器）、`src/composables/useRegionManage.ts`（城市/安全域完整 CRUD + 状态切换 + Tab 联动）、`src/api/regionManage.ts`、`src/mocks/handlers/regionManage.ts`（12条城市+4个安全域默认数据，全 CRUD Mock）、`region-manage.scss`；更新 `src/router/routes.ts` 新增 `/region` 路由，新增路由入口 `src/views/region/index.vue`；更新 `api/index.ts`、`mocks/handlers/index.ts`、`types/index.ts` 聚合导出；新增 `useRegionManage.spec.ts`（16 个测试用例全通过）。


 与样式 `src/views/download/download-list.scss`，基于 Figma `4088_301` 实现标题、筛选、列表、分页与操作区。
- **P6.7 下载逻辑**: 新增 `src/composables/useDownloadList.ts`，支持下载人可见性过滤、下载状态聚合（未下载/部分下载/已下载）、分页筛选与批量触发应用文件下载。
- **P6.7 单元测试**: 新增 `src/composables/__tests__/useDownloadList.spec.ts`，覆盖默认状态、可见性过滤、下载状态聚合与筛选重置。
- **单元测试**: 运行 `pnpm test:coverage`，14 个测试文件 53 个测试用例全部通过。
- **覆盖率报告**: 本次全量覆盖率执行通过（v8），并完成 `useDownloadList.spec.ts` 覆盖率校验。
- **工时统计模板**: 按 SSD 口径在 `docs/exec/*` 增加工时统计表字段（基线/AI工时/交互轮次/人工介入/返工/缺陷/覆盖率），并补齐缺失的 `task_P0/P1/P2/P3/P4/P7/P9/P10/P11` 模块级 exec 占位文件。
- **工时估算回填**: 基于“累计3天=24h”完成 P0~P11 第一版模块级估算回填，汇总口径更新为基线 45h、AI 24h、节省 21h、总节省率 46.7%。
- **P9.2 传输模拟逻辑**: 完善 `src/composables/useTransferSimulator.ts`，新增暂停/继续/取消与实时传输状态快照；在 `src/stores/file.ts` 集成传输状态、审批后自动开始传输及完成通知；补充 `useTransferSimulator.spec.ts`、`file.spec.ts`、`useApprovalDetail.spec.ts` 覆盖关键链路。
- **P9.1 传输进度组件**: 新增 `src/components/business/TransferProgress.vue` 与独立样式 `TransferProgress.scss`，支持状态标签、进度展示、暂停/继续/重试操作；并接入 `ApplicationDetailView.vue`、`ApprovalDetailView.vue` 展示传输进度，新增 `TransferProgress.spec.ts`，全量覆盖率更新为 16 个测试文件、65 个用例通过。
- **P9.3 传输管理页面**: 新增 `src/views/transfer/TransferManageView.vue`、`src/composables/useTransferManage.ts` 与 `transfer-manage.scss`，支持管理员查看传输任务、Tab/筛选/分页、单条暂停继续重试及批量操作；`src/views/transfers/index.vue` 由占位页切换为真实实现，新增 `useTransferManage.spec.ts`，全量覆盖率更新为 17 个测试文件、68 个用例通过。
- **P10.4 用户管理**: 新增 `src/views/admin/UserManageView.vue`、`src/views/admin/UserManageModal.vue`、`src/composables/useUserManage.ts` 与 `user-manage.scss`，支持管理员进行用户 CRUD 操作、角色分配、状态切换、重置密码；扩展 `src/api/user.ts` 与 `src/mocks/handlers/user.ts` 支持筛选参数（keyword/role/department/status）、删除用户、重置密码接口；新增 `useUserManage.spec.ts`，全量覆盖率更新为 19 个测试文件、83 个用例通过。
- **P10.5 系统配置**: 新增 `src/views/admin/SystemConfigView.vue`、`src/composables/useSystemConfig.ts`、`src/api/systemConfig.ts`、`src/mocks/handlers/systemConfig.ts` 与 `system-config.scss`，支持管理员配置传输参数（文件大小、分片、并发、有效期）、审批规则（11条层级映射、超时设置）、通知服务（邮件、短信、事件触发）、存储策略（有效期、清理周期）；更新 `src/views/settings/index.vue` 路由；新增 `useSystemConfig.spec.ts`（12个测试用例）。
- **P10 模块设计补充**: 在 `docs/tasks/task_P10.md` 补充三个缺失模块详设：P10.7 区域管理（城市-安全域映射、域配置）、P10.8 传输通道管理（加密配置、服务器管理）、P10.9 界面配置（文本配置、卡片配置、国际化、按钮显隐）。
- **P7，8，9 模块设计补充**:  完成AI 工时统计补充






- **测试修复**: 修复 `useApprovalDetail.spec.ts` 测试代码，补充 `transferTypeLabel` 返回值。
- **测试修复**: 修复 `guards.spec.ts` 避免加载含 SVG 引用的真实组件。
- **Bug 修复**: 修复申请详情页文件列表显示问题，`useApplicationDetail.ts` 现在从 `fileStore.getFilesByApplicationId()` 获取真实上传的文件。
- **Bug 修复**: 修复 `FileUpload.vue` 使用 Mock 上传的问题，现在使用真实的 `useFileUpload` 分片上传，并在上传成功后通过 `fileStore.addFile()` 将文件信息存入 Store。
- 完成 P6.1：新增选择传输类型页面 `src/views/application/SelectTypeView.vue`，按 Figma 3971_812 还原（Tab 分组 + 传输类型卡片 + 安全域渐变图标 + 审批层级标签 + hover 上浮效果）。
- 完成 P6.1：新增独立样式文件 `src/views/application/select-type.scss`，玻璃拟态卡片 + 响应式 3→2→1 列布局。
- Figma 资源同步：`public/figma/3971_812/`（19 个 SVG 文件）。
- 新增 `docs/diff/P6_Figma_Doc_Diff.md`：P6.1 Figma 与文档差异记录（7 项差异）。
- 新增 `docs/exec/task_P6_exec.md`：P6 执行记录。
- 完成 P6.4（首版）：新增申请单详情页 `src/views/application/ApplicationDetailView.vue` 与 `src/composables/useApplicationDetail.ts`，支持「申请单信息 / 文件列表」双 Tab、状态展示及按状态操作按钮。
- 完成 P6.4（抽象复用）：新增通用详情组件 `src/components/business/detail/DetailInfoSection.vue`、`DetailFileTable.vue` 及独立样式文件，供后续审批单详情复用。
- Figma 资源同步：`public/figma/3971_1904/`（9 个 SVG 文件）。
- 新增 P6.4 快速上手文档 `docs/guides/P6_ApplicationDetail_quickStart.md`：覆盖页面入口、复用组件、开发验收与 QA 回归路径。
- 完成 P8.1：新增待审批列表页面 `src/views/approvals/ApprovalListView.vue` + `src/composables/useApprovalList.ts` + `approval-list.scss`，实现 Tab 切换（待我审批/我已审批/全部审批）、筛选区域（申请单号、传输类型、申请人、申请时间）、数据表格（含审批层级标签）、分页功能。
- 完成 P8.2 & P8.4：新增审批详情页面 `src/views/approvals/ApprovalDetailView.vue` + `src/composables/useApprovalDetail.ts` + `approval-detail.scss`，实现审批时间线组件 `ApprovalTimeline.vue`、复用 `DetailInfoSection` / `DetailFileTable`、支持通过/驳回/免审操作。
- 完成 P8.3：实现审批操作完整逻辑，审批通过时判断是否为最后一级（根据 transferType），最后一级提示"已自动开始传输"，非最后一级提示"已通知下一级审批人"，操作成功后1.5秒自动跳转回审批列表。
- 新增 P8 快速上手文档 `docs/guides/P8_quickStart.md`：覆盖审批详情、审批操作、审批时间线。
- 完成 P7.1：新增通用文件上传组件 `src/components/business/FileUpload.vue` + `file-upload.scss`，支持拖拽上传、点击选择、文件列表展示（图标、名称、大小、进度、速度、状态）、文件操作（暂停/继续/重试/删除）、全局操作（全部暂停/继续/清空已完成）、并发控制（最多3个同时上传）。当前使用Mock上传逻辑，待P7.2完成后集成真实分片上传。


### Changed
- **P10 模块进度**: 更新 `docs/tasks/task_P10.md`、`docs/TaskList.md`、`docs/exec/task_P10_exec.md` 与 `docs/exec/task_project_progress_summary_2026-03-06.md`，同步 `P10.3` 完成状态，并记录 `P10.1 / P10.2` 暂缓、后续优先 `P10.4`。
- **头部通知刷新**: 更新 `src/components/common/AppHeader.vue`，在切换账号后自动拉取当前用户通知，确保头部未读角标同步刷新。
- P6.1 补齐：按 Figma 3830_3 补充绿区传出缺失的 2 个传输类型（绿区传到外网、绿区传到海思红区），绿区传出 Tab 现有 5 种类型。

- P6.1 补齐：图标资源统一切换到 `public/figma/3830_3/`，描述文案对齐 Figma（非研发到非研发/研发/外网/红区/海思红区）。
- P6.1 补齐：渐变色对齐 Figma 3830_3（黄区 `#F2B313→#EE7214`、红区 `#FF6900→#FB2C36`），新增外网渐变 `#2B7FFF→#00BBA7`、海思红区渐变 `#AD46FF→#F6339A`。
- P6.1 补齐：网格布局从 3 列调整为 5 列（≥1400px），新增 1400px 响应式断点。
- 更新 `src/router/routes.ts`：新增 `/application/select-type`（选择传输类型）和 `/application/create`（创建申请单）路由。
- 更新 `src/router/routes.ts`：新增 `/application/:id`（申请单详情）路由。
- 更新 `src/views/application/ApplicationListView.vue`：查看动作改为跳转申请单详情页。
- 更新 `src/router/routes.ts`：新增 `/approvals/:id`（审批详情）路由，更新 `/approvals` 路由指向 `ApprovalListView.vue`。
- 更新 `src/api/approval.ts`、`src/stores/approval.ts`、`src/mocks/handlers/approval.ts`：新增审批历史查询链路 `GET /approvals/:id/history`。
- 删除审批管理旧占位文件：`src/views/approvals/index.vue` 与 `approval-index.scss`，已被列表页替代。
- 更新 `docs/tasks/task_P8.md`：勾选 P8.1（待审批列表）、P8.2（审批详情）、P8.3（审批操作）、P8.4（审批时间线）已完成项，P8 模块全部完成。
- 更新 `src/composables/useApprovalDetail.ts`：新增 `isLastLevel` 方法判断审批层级，审批通过后根据是否最后一级展示不同提示信息，操作成功后1.5秒自动跳转回审批列表。
- 更新 `src/mocks/handlers/approval.ts`：实现审批层级判断逻辑，审批通过时若非最后一级则 `currentApprovalLevel + 1` 并保持 `pending_approval` 状态，最后一级则状态改为 `approved`。
- 更新 `docs/tasks/task_P7.md`：勾选 P7.1（文件上传组件）已完成项。


### Fixed
- 修复“我的申请单”页首刷后删除/撤回弹框样式异常问题：在 `src/main.ts` 全局补充引入 `@arco-design/web-vue/es/modal/style/css.js` 与 `@arco-design/web-vue/es/message/style/css.js`，确保 `Modal.*` / `Message` 首次触发即可正确渲染。
- 修复 `src/components/business/__tests__/FileUpload.spec.ts` 未初始化 Pinia 导致 `pnpm test:coverage` 失败的问题，在 `beforeEach` 中补充 `setActivePinia(createPinia())`。


### Added

- 完成 P5.1：新增登录页面 `src/views/auth/LoginView.vue`，按 Figma 3971_1023 还原（居中卡片 + Logo + 渐变标题 + Arco 表单 + Demo 快速体验表格）。
- 完成 P5.1：新增独立样式文件 `src/views/auth/login.scss`，复用 mixin。
- 完成 P5.2：新增 `src/composables/useLogin.ts`（表单校验 / 登录 / 快速登录 / 记住我 / 角色跳转）。
- 完成 P5.3：新增 `src/components/common/RoleSwitcher.vue` 角色切换器组件（仅开发环境渲染，a-dropdown + 角色标签颜色区分）。
- 完成 P5.3：新增 `src/components/common/styles/role-switcher.scss` 独立样式。
- Figma 资源同步：`public/figma/3971_1023/`（1.svg / 2.svg / 3.svg）。

### Changed
- 更新 `src/router/routes.ts`：登录页组件指向 `auth/LoginView.vue`。
- 更新 `src/components/common/AppHeader.vue`：将旧的 Demo select 切换器替换为 `RoleSwitcher` 组件。
- 更新 `src/components/common/styles/app-header.scss`：移除旧 `.role-switcher` / `.role-switch` 样式。
- 重组 `docs/` 目录结构：新增 `design/`（设计文档+UML）、`diff/`（Figma/文档差异）、`exec/`（执行记录）、`guides/`（quickStart 指南）四个子目录，原有散落文件归档至对应目录。
- 新增 `docs/diff/P5_Figma_Doc_Diff.md`：P5 登录模块 Figma 与文档差异记录。
- 新增 `docs/exec/task_P5_exec.md`：P5 执行记录（进度/验收/产出清单）。

### Added
- 完成 P3：新增状态管理与接口层文件 `src/stores/auth.ts`、`application.ts`、`file.ts`、`approval.ts`、`notification.ts`、`src/stores/index.ts`。
- 完成 P3：新增业务 API 文件 `src/api/application.ts`、`approval.ts`、`notification.ts`，并更新 `src/api/index.ts` 聚合导出。
- 完成 P3：新增 Store 单元测试 `src/stores/__tests__/auth.spec.ts`、`application.spec.ts`、`file.spec.ts`、`approval.spec.ts`、`notification.spec.ts`。
- 新增 P3 快速上手文档 `docs/P3_Store_quickStart.md`，用于指导状态管理层接入与使用。
- 完成 P4：新增路由与守卫实现 `src/router/routes.ts`、`src/router/index.ts`、`src/router/guards.ts` 与守卫单测 `src/router/__tests__/guards.spec.ts`。
- 完成 P4：新增布局与公共组件 `src/layouts/BlankLayout.vue`、`src/layouts/DefaultLayout.vue`、`src/components/common/AppHeader.vue`、`AppSidebar.vue`、`PageContainer.vue`。
- 完成 P4：新增页面实现 `src/views/dashboard/index.vue`（Figma 还原）及 `login/applications/approvals/transfer/transfers/users/settings/logs/profile/notifications/error` 页面文件。
- 完成 P4：新增权限指令 `src/directives/permission.ts`，并在 `src/main.ts` 注册 `v-permission` 与 `v-role`。
- 新增 P4 文档与高保真差异记录 `docs/P4_Figma_Doc_Diff.md`。



### Changed
- 更新 `qtrans-frontend/src/main.ts`：注册 `pinia` 与 `pinia-plugin-persistedstate`。
- 更新 `docs/tasks/task_P3.md`：子任务勾选与完成标志更新为 `[√]`。
- 更新 P4 布局细节：`AppSidebar` 修复图标渲染并将折叠按钮下沉至侧栏底部，`AppHeader` 调整为 `Demo切换 -> 通知 -> 姓名下拉`，并将样式拆分为独立 `scss` 文件复用 mixin。
- 更新头部用户区样式：按 Figma `3971_1105` 重构右上用户名与下拉面板样式，并将 `PageContainer.vue` 样式拆分至独立 `scss` 文件并复用 mixin。
- 完成 P6.2：新增 `CreateApplicationView` 三步创建流程与 `useApplicationForm`（自动草稿、条件字段、离开确认、提交成功态），并按 Figma `3960_2183` 对齐成功页样式。
- 完成 P6.2 增量补齐（Figma `3879_3344` / `3883_4645`）：Step1 拆分“基本信息/申请信息”、基本信息只读化、补充抄送人与右侧“注意事项/最近传输选择（一键复制）”、并对齐发起申请高亮进度与表单样式。
- 完成 P6.2 二次补齐：接入 P2.2 部门树 Mock 到发起申请部门选择器；布局改为固定顶栏+固定侧栏+内容区独立滚动；申请页底部操作横栏改为固定吸底并修正背景填充色。
- 完成 P6.2 三次补齐：修复步骤条首步高亮命中（active/process 双选择器）；将部门选择改为点击弹窗树选择并回填；吸底操作栏通过 `width: 100%` + `box-sizing: border-box` 贴齐页面底部与左侧边缘。
- 完成 P6.2 Step2 重构（基于 Figma 3883_5466）：双表格展示「传输任务」与「已上传文件列表」，支持实时进度条动画、批量暂停/继续/删除、文件类型图标映射、Mock 上传模拟，样式对齐 Figma 表格卡片与渐变进度条。
- 完成 P6.2 组件拆分重构：将 `CreateApplicationView.vue`（768 行）拆分为三个独立子组件 `StepOneBasicInfo.vue`（基本信息+申请信息）、`StepTwoUploadFile.vue`（双表格上传）、`StepThreeSubmitSuccess.vue`（提交成功），主组件精简至 231 行，提升可维护性。
- 修复 Figma 3883_5466 图标渲染问题：从 `assets/CodeBubbyAssets/3883_5466/` 复制 47 个 SVG 文件到 `public/figma/3883_5466/`，确保所有按钮与文件类型图标正确显示。
- 修复 P6.2 Step1 样式回归：`CreateApplicationView.vue` 改为非 `scoped` 引入 `create-application.scss`，恢复拆分后子组件（`StepOneBasicInfo.vue`）的布局与表单样式。
- 修复 P6.2 Step1 细节样式：统一「基本信息/申请信息」标题字体、恢复申请信息选择类控件默认白底、底部操作栏按钮改为左对齐，并将「包含客户网络数据」从按钮单选改回普通单选以对齐 Figma。
- 修复 P6.2 Step1 样式继承链路：`CreateApplicationView.vue` 改为脚本显式引入 `create-application.scss`，并将样式中的 `:deep(...)` 批量替换为普通后代选择器，恢复标题、表单控件与步骤条样式命中。
- 修复 P6.2 Step1 表单状态回归：为国家城市级联选择器启用 `path-mode` 并规范化数组值，解决选择后仍触发“请选择源国家/城市”；同时恢复 radio/checkbox 的 checked 填充态与勾选态显示。
- 修复 P6.2 Step1 国家/城市校验残留问题：补充 `a-cascader` 返回值归一化逻辑（兼容 string/number/array），避免有效选择被错误置空后重复触发“请选择源国家/城市”。
- 修复 P6.2 Step2 按钮图标未渲染：`StepTwoUploadFile.vue` 显式引入 Arco Icon 组件并替换 `icon-*` 标签，恢复上传按钮与表格操作列按钮图标显示。
- 修复 P6.2 Step3 视觉问题：成功态图标改为 `3.svg + 4.svg` 叠加显示对号；步骤条 `current` 改为 `currentStep + 1` 并补齐 `finish/process/active` 高亮；成功页底部按钮样式对齐 Figma（圆角、字体、色值与宽度）。
- 完成 P6.3 申请单列表页面：新增 `ApplicationListView.vue` + `useApplicationList.ts` + `application-list.scss`，实现搜索/状态筛选/高级筛选（时间范围）/表格分页/状态标签/草稿编辑删除与待审批撤回，并接入 Figma `3961_3234` 资源（复制 33 个 SVG 到 `public/figma/3961_3234/`）。
- 新增 P6.3 快速上手文档 `docs/guides/P6_ApplicationList_quickStart.md`：覆盖入口路由、筛选/分页/导出/操作行为、代码结构与扩展点。
- 修复 P6.3 申请单列表页问题：增加历史草稿字段归一化（兼容 string/array）避免 `sourceCity.join` 报错；补充申请原因/下载人/城市字段的空值兜底显示；操作列由文字按钮改为 Figma 图标按钮并对齐样式。
- 修复 P6.3 操作列可用性：为查看/编辑/删除/撤回按钮补充悬浮提示（tooltip）；将高保真缺失或重复语义图标替换为 Arco Icon（`IconEye`/`IconEdit`/`IconDelete`/`IconUndo`），提升可辨识度。


















## [2026-03-05]

### Added
- 完成 P7.2 文件分片上传逻辑：新增 `useFileUpload` composable，实现基于 IndexedDB 的断点续传、暂停/继续/取消功能，支持 5MB 分片上传，集成 AbortController 请求控制和实时进度追踪。
- 完成 P7.3 断点续传实现：基于 IndexedDB 的分片记录机制，上传前自动检测已上传分片并跳过，恢复上传进度，支持暂停后继续、关闭浏览器后恢复上传。
- 完成 P7.4 文件列表组件：新增 `FileList.vue`，支持 upload/download/view 三种模式，展示文件名、大小、上传时间、状态，提供删除/下载/预览操作，底部统计文件总数和总大小。
- 新增文件上传 API (`src/api/file.ts`)：uploadChunk（分片上传）、mergeChunks（分片合并）、deleteFile（删除文件）、getFileList（文件列表）。
- 新增 MSW 文件上传 Mock Handlers (`src/mocks/handlers/file.ts`)：模拟分片上传、合并、删除和列表查询接口。

### Changed
- 更新 `src/api/index.ts`：导出 file API 模块。

## [2026-03-04]

### Added
- 完成 P2.1：新增 `qtrans-frontend/src/mocks/db.ts`（Dexie 数据库与 `fileChunks`/`fileMetas` 表）。
- 完成 P2.1：新增 `qtrans-frontend/src/composables/useFileChunk.ts`（分片计算、切片、SHA-256 哈希）。
- 完成 P2.2：新增 Mock 数据文件 `users.ts`、`departments.ts`、`cities.ts`、`applications.ts`、`index.ts`、`demo-init.ts`。
- 完成 P2.3：新增 MSW 启动与拦截实现 `src/mocks/browser.ts`、`src/mocks/handlers/*`。
- 完成 P2.3：生成 `qtrans-frontend/public/mockServiceWorker.js`。
- 完成 P2.4：新增 `qtrans-frontend/src/composables/useFileUpload.ts` 与 `useTransferSimulator.ts`。

### Changed
- 更新 `qtrans-frontend/src/main.ts`：开发环境按 `VITE_MOCK_ENABLED` 条件启动 MSW。
- 更新 `qtrans-frontend/tsconfig.app.json`：补充 `baseUrl` 与 `@/*` 路径别名。
- 更新 `qtrans-frontend/src/utils/request.ts`：修正响应拦截器返回类型以通过 TypeScript 构建。
- 更新 `qtrans-frontend/package.json`：记录 `msw.workerDirectory`。
- 更新 `docs/tasks/task_P2.md`：已执行子任务由 `[ ]` 勾选为 `[√]`。

### Fixed
- 修复 `src/mocks/handlers/application.ts`、`src/mocks/handlers/user.ts` 在 `strict + noUncheckedIndexedAccess` 下的类型错误。

## [2026-03-03]
### Added
- 完成 P1.1：新增类型定义文件 `src/types/common.ts`、`user.ts`、`application.ts`、`file.ts`、`approval.ts`、`notification.ts`、`index.ts`。
- 完成 P1.2：新增工具函数文件 `src/utils/format.ts`、`validate.ts`、`storage.ts`、`file.ts`、`index.ts` 及对应单测。
- 完成 P1.3：新增 `src/utils/constants.ts`，统一维护业务常量与字面量类型。
- 完成 P1.4：新增 Axios 请求封装 `src/utils/request.ts`，并在 `src/api/auth.ts` 完成首个业务接口接入。

### Fixed
- 修复 `src/utils/__tests__/format.spec.ts` 与 `src/utils/format.ts` 在文件大小格式断言上的不一致问题（`1 KB/1 MB`）。

## [2026-03-02]
### Added
- 完成 P0：初始化 `Vue3 + TypeScript + Vite` 项目骨架，并配置 `@` 路径别名与严格模式。
- 完成 P0：新增基础目录结构（`src/api`、`src/components`、`src/composables`、`src/mocks`、`src/router`、`src/stores`、`src/types`、`src/utils`、`src/views/*`）。
- 完成 P0：新增工程化能力（ESLint/Prettier/Husky/lint-staged）与环境变量文件（`.env.development`、`.env.production`、`src/env.d.ts`）。


