# Changelog

All notable changes to this project will be documented in this file.

The format is based on Keep a Changelog.

## [Unreleased]

### Added
- 完成 P6.1：新增选择传输类型页面 `src/views/application/SelectTypeView.vue`，按 Figma 3971_812 还原（Tab 分组 + 传输类型卡片 + 安全域渐变图标 + 审批层级标签 + hover 上浮效果）。
- 完成 P6.1：新增独立样式文件 `src/views/application/select-type.scss`，玻璃拟态卡片 + 响应式 3→2→1 列布局。
- Figma 资源同步：`public/figma/3971_812/`（19 个 SVG 文件）。
- 新增 `docs/diff/P6_Figma_Doc_Diff.md`：P6.1 Figma 与文档差异记录（7 项差异）。
- 新增 `docs/exec/task_P6_exec.md`：P6 执行记录。

### Changed
- P6.1 补齐：按 Figma 3830_3 补充绿区传出缺失的 2 个传输类型（绿区传到外网、绿区传到海思红区），绿区传出 Tab 现有 5 种类型。
- P6.1 补齐：图标资源统一切换到 `public/figma/3830_3/`，描述文案对齐 Figma（非研发到非研发/研发/外网/红区/海思红区）。
- P6.1 补齐：渐变色对齐 Figma 3830_3（黄区 `#F2B313→#EE7214`、红区 `#FF6900→#FB2C36`），新增外网渐变 `#2B7FFF→#00BBA7`、海思红区渐变 `#AD46FF→#F6339A`。
- P6.1 补齐：网格布局从 3 列调整为 5 列（≥1400px），新增 1400px 响应式断点。
- 更新 `src/router/routes.ts`：新增 `/application/select-type`（选择传输类型）和 `/application/create`（创建申请单）路由。

### Added
- 完成 P5.1：新增登录页面 `src/views/auth/LoginView.vue`，按 Figma 3971_1023 还原（居中卡片 + Logo + 渐变标题 + Arco 表单 + Demo 快速体验表格）。
- 完成 P5.1：新增独立样式文件 `src/views/auth/login.scss`，复用 mixin。
- 完成 P5.2：新增 `src/composables/useLogin.ts`（表单校验 / 登录 / 快速登录 / 记住我 / 角色跳转）。
- 完成 P5.3：新增 `src/components/common/RoleSwitcher.vue` 角色切换器组件（仅开发环境渲染，a-dropdown + 角色标签颜色区分）。
- 完成 P5.3：新增 `src/components/common/styles/role-switcher.scss` 独立样式。
- Figma 资源同步：`public/figma/3971_1023/`（1.svg / 2.svg / 3.svg）。

### Changed
- 更新 `src/router/routes.ts`：登录页组件指向 `auth/LoginView.vue`。
- 更新 `src/components/common/AppHeader.vue`：将旧的 Demo select 切换器替换为 `RoleSwitcher` 组件。
- 更新 `src/components/common/styles/app-header.scss`：移除旧 `.role-switcher` / `.role-switch` 样式。
- 重组 `docs/` 目录结构：新增 `design/`（设计文档+UML）、`diff/`（Figma/文档差异）、`exec/`（执行记录）、`guides/`（quickStart 指南）四个子目录，原有散落文件归档至对应目录。
- 新增 `docs/diff/P5_Figma_Doc_Diff.md`：P5 登录模块 Figma 与文档差异记录。
- 新增 `docs/exec/task_P5_exec.md`：P5 执行记录（进度/验收/产出清单）。

### Added
- 完成 P3：新增状态管理与接口层文件 `src/stores/auth.ts`、`application.ts`、`file.ts`、`approval.ts`、`notification.ts`、`src/stores/index.ts`。
- 完成 P3：新增业务 API 文件 `src/api/application.ts`、`approval.ts`、`notification.ts`，并更新 `src/api/index.ts` 聚合导出。
- 完成 P3：新增 Store 单元测试 `src/stores/__tests__/auth.spec.ts`、`application.spec.ts`、`file.spec.ts`、`approval.spec.ts`、`notification.spec.ts`。
- 新增 P3 快速上手文档 `docs/P3_Store_quickStart.md`，用于指导状态管理层接入与使用。
- 完成 P4：新增路由与守卫实现 `src/router/routes.ts`、`src/router/index.ts`、`src/router/guards.ts` 与守卫单测 `src/router/__tests__/guards.spec.ts`。
- 完成 P4：新增布局与公共组件 `src/layouts/BlankLayout.vue`、`src/layouts/DefaultLayout.vue`、`src/components/common/AppHeader.vue`、`AppSidebar.vue`、`PageContainer.vue`。
- 完成 P4：新增页面实现 `src/views/dashboard/index.vue`（Figma 还原）及 `login/applications/approvals/transfer/transfers/users/settings/logs/profile/notifications/error` 页面文件。
- 完成 P4：新增权限指令 `src/directives/permission.ts`，并在 `src/main.ts` 注册 `v-permission` 与 `v-role`。
- 新增 P4 文档与高保真差异记录 `docs/P4_Figma_Doc_Diff.md`。



### Changed
- 更新 `qtrans-frontend/src/main.ts`：注册 `pinia` 与 `pinia-plugin-persistedstate`。
- 更新 `docs/tasks/task_P3.md`：子任务勾选与完成标志更新为 `[√]`。
- 更新 P4 布局细节：`AppSidebar` 修复图标渲染并将折叠按钮下沉至侧栏底部，`AppHeader` 调整为 `Demo切换 -> 通知 -> 姓名下拉`，并将样式拆分为独立 `scss` 文件复用 mixin。
- 更新头部用户区样式：按 Figma `3971_1105` 重构右上用户名与下拉面板样式，并将 `PageContainer.vue` 样式拆分至独立 `scss` 文件并复用 mixin。
- 完成 P6.2：新增 `CreateApplicationView` 三步创建流程与 `useApplicationForm`（自动草稿、条件字段、离开确认、提交成功态），并按 Figma `3960_2183` 对齐成功页样式。
- 完成 P6.2 增量补齐（Figma `3879_3344` / `3883_4645`）：Step1 拆分“基本信息/申请信息”、基本信息只读化、补充抄送人与右侧“注意事项/最近传输选择（一键复制）”、并对齐发起申请高亮进度与表单样式。
- 完成 P6.2 二次补齐：接入 P2.2 部门树 Mock 到发起申请部门选择器；布局改为固定顶栏+固定侧栏+内容区独立滚动；申请页底部操作横栏改为固定吸底并修正背景填充色。
- 完成 P6.2 三次补齐：修复步骤条首步高亮命中（active/process 双选择器）；将部门选择改为点击弹窗树选择并回填；吸底操作栏通过 `width: 100%` + `box-sizing: border-box` 贴齐页面底部与左侧边缘。
- 完成 P6.2 Step2 重构（基于 Figma 3883_5466）：双表格展示「传输任务」与「已上传文件列表」，支持实时进度条动画、批量暂停/继续/删除、文件类型图标映射、Mock 上传模拟，样式对齐 Figma 表格卡片与渐变进度条。
- 完成 P6.2 组件拆分重构：将 `CreateApplicationView.vue`（768 行）拆分为三个独立子组件 `StepOneBasicInfo.vue`（基本信息+申请信息）、`StepTwoUploadFile.vue`（双表格上传）、`StepThreeSubmitSuccess.vue`（提交成功），主组件精简至 231 行，提升可维护性。
- 修复 Figma 3883_5466 图标渲染问题：从 `assets/CodeBubbyAssets/3883_5466/` 复制 47 个 SVG 文件到 `public/figma/3883_5466/`，确保所有按钮与文件类型图标正确显示。
- 修复 P6.2 Step1 样式回归：`CreateApplicationView.vue` 改为非 `scoped` 引入 `create-application.scss`，恢复拆分后子组件（`StepOneBasicInfo.vue`）的布局与表单样式。
- 修复 P6.2 Step1 细节样式：统一「基本信息/申请信息」标题字体、恢复申请信息选择类控件默认白底、底部操作栏按钮改为左对齐，并将「包含客户网络数据」从按钮单选改回普通单选以对齐 Figma。
- 修复 P6.2 Step1 样式继承链路：`CreateApplicationView.vue` 改为脚本显式引入 `create-application.scss`，并将样式中的 `:deep(...)` 批量替换为普通后代选择器，恢复标题、表单控件与步骤条样式命中。
- 修复 P6.2 Step1 表单状态回归：为国家城市级联选择器启用 `path-mode` 并规范化数组值，解决选择后仍触发“请选择源国家/城市”；同时恢复 radio/checkbox 的 checked 填充态与勾选态显示。
- 修复 P6.2 Step1 国家/城市校验残留问题：补充 `a-cascader` 返回值归一化逻辑（兼容 string/number/array），避免有效选择被错误置空后重复触发“请选择源国家/城市”。












## [2026-03-04]

### Added
- 完成 P2.1：新增 `qtrans-frontend/src/mocks/db.ts`（Dexie 数据库与 `fileChunks`/`fileMetas` 表）。
- 完成 P2.1：新增 `qtrans-frontend/src/composables/useFileChunk.ts`（分片计算、切片、SHA-256 哈希）。
- 完成 P2.2：新增 Mock 数据文件 `users.ts`、`departments.ts`、`cities.ts`、`applications.ts`、`index.ts`、`demo-init.ts`。
- 完成 P2.3：新增 MSW 启动与拦截实现 `src/mocks/browser.ts`、`src/mocks/handlers/*`。
- 完成 P2.3：生成 `qtrans-frontend/public/mockServiceWorker.js`。
- 完成 P2.4：新增 `qtrans-frontend/src/composables/useFileUpload.ts` 与 `useTransferSimulator.ts`。

### Changed
- 更新 `qtrans-frontend/src/main.ts`：开发环境按 `VITE_MOCK_ENABLED` 条件启动 MSW。
- 更新 `qtrans-frontend/tsconfig.app.json`：补充 `baseUrl` 与 `@/*` 路径别名。
- 更新 `qtrans-frontend/src/utils/request.ts`：修正响应拦截器返回类型以通过 TypeScript 构建。
- 更新 `qtrans-frontend/package.json`：记录 `msw.workerDirectory`。
- 更新 `docs/tasks/task_P2.md`：已执行子任务由 `[ ]` 勾选为 `[√]`。

### Fixed
- 修复 `src/mocks/handlers/application.ts`、`src/mocks/handlers/user.ts` 在 `strict + noUncheckedIndexedAccess` 下的类型错误。

## [2026-03-03]
### Added
- 完成 P1.1：新增类型定义文件 `src/types/common.ts`、`user.ts`、`application.ts`、`file.ts`、`approval.ts`、`notification.ts`、`index.ts`。
- 完成 P1.2：新增工具函数文件 `src/utils/format.ts`、`validate.ts`、`storage.ts`、`file.ts`、`index.ts` 及对应单测。
- 完成 P1.3：新增 `src/utils/constants.ts`，统一维护业务常量与字面量类型。
- 完成 P1.4：新增 Axios 请求封装 `src/utils/request.ts`，并在 `src/api/auth.ts` 完成首个业务接口接入。

### Fixed
- 修复 `src/utils/__tests__/format.spec.ts` 与 `src/utils/format.ts` 在文件大小格式断言上的不一致问题（`1 KB/1 MB`）。

## [2026-03-02]
### Added
- 完成 P0：初始化 `Vue3 + TypeScript + Vite` 项目骨架，并配置 `@` 路径别名与严格模式。
- 完成 P0：新增基础目录结构（`src/api`、`src/components`、`src/composables`、`src/mocks`、`src/router`、`src/stores`、`src/types`、`src/utils`、`src/views/*`）。
- 完成 P0：新增工程化能力（ESLint/Prettier/Husky/lint-staged）与环境变量文件（`.env.development`、`.env.production`、`src/env.d.ts`）。


