# Changelog

All notable changes to this project will be documented in this file.

The format is based on Keep a Changelog.

## [Unreleased]

### Changed - 2026-04-03

#### 审批接口响应处理修复

**问题修复**:
- 🐛 修复审批通过/驳回接口响应处理异常 - 后端返回 `{"code":"200"}` 被误判为错误
- 🐛 修复点击审批按钮后页面无反应的问题

**根本原因**:
- `rawClient` 响应拦截器只检查了 `'success'` 和 `'0'` 成功状态码
- 未包含 `'200'`，导致成功响应被 Promise.reject

**解决方案**:
- ✨ 在成功状态码列表中添加 `'200'`
- ♻️ 使用数组 `includes` 方法优化判断逻辑
- 📝 新增文档: docs/tasks/task_fix_approval_response.md
- 📝 新增文档: docs/exec/task_fix_approval_response_exec.md

#### 审批详情页自动切换 Tab 优化

**用户体验优化**:
- ✨ 有资产检测结果且未确认时，自动切换到资产检测 Tab
- ✨ 审批人能立即看到需要确认的内容，避免不知所措
- 📝 新增文档: docs/tasks/task_auto_switch_tab.md

#### 表格列宽拖拽优化

**问题修复**:
- 🐛 修复拖拽手柄不可见问题 - 增加列分隔线和明显的拖拽图标
- 🐛 修复拖拽响应不准确问题 - 移除 CSS min-width/max-width 约束,避免与 columns 配置冲突

**样式优化**:
- ✨ 表头增加列分隔线(浅灰色竖线),明确列边界
- ✨ 拖拽手柄宽度从 4px 增加到 10px,提高可点击性
- ✨ 拖拽手柄默认显示浅灰色竖线图标
- ✨ hover 时显示蓝色背景和高亮图标,提供视觉反馈
- ✨ 拖拽激活时图标变长,背景加深,提供进度反馈

**技术改进**:
- ♻️ 移除 CSS 中的列宽定义类(.col-file-name 等)
- ♻️ 完全依赖 columns 配置管理列宽,避免冲突
- 📝 新增文档: docs/tasks/task_column_resize_optimization.md
- 📝 新增文档: docs/exec/task_column_resize_exec.md

#### 资产检测结果组件重构

**重构为 a-table**:
- 使用 Arco Design 的 `a-table` 组件替代原生 table 标签
- 减少约 50 行代码，提升可维护性
- 获得更多内置功能：斑马纹、排序、筛选等

**新增功能**:
- ✅ **列宽拖拽** - 使用 `column-resizable` 属性支持列宽拖拽调整
- ✅ **内置分页器** - 不再需要手动处理分页 UI
- ✅ **Tooltip 提示** - 文件名和文件路径列自动显示完整内容
- ✅ **斑马纹** - 自动交替行背景色，提升可读性
- ✅ **Loading 状态** - 表格级别的加载状态

**新增展示列**:
- 新增"文件大小"列（fileSizeUnit）- 显示每个文件的大小
- 新增"压缩层级"列（unzipLevel）- 显示压缩包解压层级
- 新增"文件路径"列（filePath）- 显示完整文件路径

**列宽优化**:
- 文件名称列：180px，可拖拽调整
- 文件类型列：120px，可拖拽调整
- 文件大小列：100px（右对齐），可拖拽调整
- 密级列：80px，可拖拽调整
- 压缩层级列：90px（居中对齐），可拖拽调整
- 文件路径列：220px，可拖拽调整
- 操作列：100px（固定右侧）

**类型定义更新**:
- `KiaFileItem` 新增可选字段：`fileSizeUnit`、`unzipLevel`、`filePath`
- `KiaKeyFileItem` 新增可选字段：`fileSizeUnit`、`unzipLevel`、`filePath`

**设计改进**:
- 遵循前端设计规范，避免列宽过长导致信息密度不足
- 右对齐数字列（文件大小），居中对齐枚举列（压缩层级）
- 使用插槽（slot）自定义渲染，代码更清晰
- 拖拽手柄悬停时高亮，提升可发现性

### Fixed - 2026-04-03

#### 资产检测结果表格优化

**新增展示列**:
- 新增"文件大小"列（fileSizeUnit）- 显示每个文件的大小
- 新增"压缩层级"列（unzipLevel）- 显示压缩包解压层级
- 新增"文件路径"列（filePath）- 显示完整文件路径

**列宽优化**:
- 文件名称列：从 280-400px 缩小到 160-220px
- 文件类型列：从 180px 缩小到 120px
- 文件大小列：100px，右对齐
- 密级列：从 100px 缩小到 80px
- 压缩层级列：80px，居中对齐
- 文件路径列：200-300px

**类型定义更新**:
- `KiaFileItem` 新增可选字段：`fileSizeUnit`、`unzipLevel`、`filePath`
- `KiaKeyFileItem` 新增可选字段：`fileSizeUnit`、`unzipLevel`、`filePath`

**设计改进**:
- 遵循前端设计规范，避免列宽过长导致信息密度不足
- 右对齐数字列（文件大小），居中对齐枚举列（压缩层级）
- 文本溢出时显示 tooltip，保证信息完整

### Changed - 2026-04-03

#### 资产检测结果模块重构

**重大变更**: 资产检测从独立卡片改为第三个 Tab,与"申请单信息""文件列表"并列

**新增功能**:
- **新增接口** `findKiaResultList` - 分页查询所有检测文件
  - 支持分页查询（pageNum, pageSize）
  - 支持文件类型筛选（fileType）
  - 支持关键字查询（fileName）
  
- **新增组件** `AssetDetectionTab.vue` - 新的 Tab 模式组件
  - 统计概览（总数量、总大小、分类统计）
  - 文件列表展示（支持分页）
  - 筛选功能（文件类型筛选）
  - 关键字查询功能（文件名搜索）
  - 子 Tab 切换（文件列表/关键资产）
  - 两级确认逻辑（文件确认 + 关键资产确认）

- **新增组件** `AssetFilterBar.vue` - 筛选栏组件
  - 文件类型下拉筛选
  - 文件名关键字搜索
  - 重置功能

**功能优化**:
- **确认逻辑重构**:
  - 先确认所有文件（逐条确认）
  - 确认完后如果有关键资产，再确认关键资产
  - 关键资产未全部确认时，操作按钮置灰
  
- **接口职责调整**:
  - `findKiaResultKeyList` - 仅查询 fileType=4 的关键资产
  - `findKiaResultList` - 查询所有检测文件（支持分页和筛选）

- **样式优化**:
  - Tab 按钮支持自适应宽度（auto-fit）
  - 资产检测结果 Tab 使用警醒样式（橙色背景 + 脉冲动画）
  - 子 Tab 支持切换动画

**类型定义更新**:
- 新增 `KiaFileItem` - 检测文件项
- 新增 `KiaResultListRequest` - 分页查询请求参数
- 新增 `KiaResultListResponse` - 分页查询响应

**Composable 重构** (`useAssetDetection.ts`):
- 新增分页查询支持
- 新增筛选和关键字查询支持
- 新增两级确认状态管理
- 新增 `canOperate` 计算属性（按钮置灰逻辑）
- 新增 `categoryStats` 计算属性（分类统计）

**页面集成**:
- `ApplicationDetailView.vue` - 集成新的 Tab 模式
- `ApprovalDetailView.vue` - 集成新的 Tab 模式
- `activeTab` 类型从 `'info' | 'files'` 扩展为 `'info' | 'files' | 'detection'`

**删除文件**:
- 删除旧的 `AssetDetectionResult.vue` 组件（已被 `AssetDetectionTab.vue` 替代）

### Fixed - 2026-04-02
- **TransWeb 认证改为 Cookie 同步**: `src/api/transWebService.ts` 在保留 `headers.token` / `Authorization` 的同时，新增将主站 `token` 同步写入 Cookie 的逻辑，并在下载 `fetch` 请求中保持同源 Cookie 发送，适配后端新的 Cookie 鉴权要求。

### Added - 2026-04-02


### Added - 2026-04-02

#### 资产检测结果模块
- **新增组件** `AssetDetectionResult.vue` - 资产检测结果展示组件
  - 统计卡片展示关键资产数量和总大小
  - 可展开的文件列表（文件名、文件类型、密级）
  - 支持审批确认功能（逐条确认后才能操作）
  - 通过 props 接收数据，支持父组件状态共享

- **新增 API** `src/api/assetDetection.ts`
  - `getKiaResultCount` - 查询关键资产统计
  - `getKiaResultKeyList` - 查询关键资产列表
  - `getSecretLevelList` - 查询密级枚举

- **新增 Composable** `useAssetDetection.ts`
  - 封装资产检测数据获取逻辑
  - 提供确认状态管理
  - 提供 `processedFileList`（带展示名称的文件列表）

- **新增常量** `src/constants/fileType.ts`
  - fileType 枚举映射（中文名称）

- **新增类型** `src/types/assetDetection.ts`
  - KiaResultItem、KiaResultCountResponse
  - KiaKeyFileItem、SecretLevelItem

#### 申请单详情页面优化
- 集成资产检测结果展示（独立卡片）
- "继续上传文件"按钮仅在"创建申请单"和"文件上传"状态展示
- **关键资产确认逻辑**：
  - 当 `count > 0`（有关键资产）时，按钮置灰
  - 用户确认完所有关键资产后，按钮恢复可点击

#### 审批单详情页面优化
- 集成资产检测结果展示（独立卡片）
- **关键资产确认逻辑**：
  - 关键资产需逐条确认
  - 全部确认后审批按钮（通过/驳回）才可点击
  - 显示确认进度提示


## [Unreleased]

### Fixed
- **tenant/admin 开发模式文根修复**: 为 `.env.tenant.development` / `.env.admin.development` 补齐 `VITE_APP_TYPE` 与 `VITE_APP_TITLE`，并在 `vite.config.ts` 增加基于 `mode` 的兜底识别，修复 `dev:admin` 仍落到 `/tenant/`、路由/菜单按租户面过滤的问题。
- **共享头部跨面可用性修复**: 将 `个人中心` 与 `通知中心` 路由改为 `shared`，并为 `auth` / `notification` 持久化增加按 `appType` 隔离的 storage key，避免租户面与管理面状态串用。
- **通知请求报错降级处理**: `notification` store 在拉取通知失败时改为优雅回退，避免头部初始化时产生未捕获 Promise 报错。
- **静态资源 base 适配补齐**: 修复 `LoginView`、`ApplicationListView`、`CreateApplicationView`、`ApplicationDetailView`、`ApprovalDetailView`、`Dashboard` 等页面残留的 `/figma/...` 绝对路径，统一走 `assetPath()`，兼容 `/tenant/` 与 `/admin/` 文根。
- **MSW 与本地接口文根对齐修复**: `worker.start()` 改为按 `BASE_URL` 注册 `mockServiceWorker.js` 和 scope，mock handlers 统一走带文根前缀的 `baseHttp`，并在 `vite.config.ts` 为 `/tenant/*`、`/admin/*` 补齐代理转发，修复开发环境下 `mockServiceWorker.js` 404 与 `/api/*` 请求未命中 mock/代理的问题。
- **跨文根请求客户端补齐**: `request.ts`、`transWebService.ts` 中的本地相对接口统一经 `assetPath()` 归一化，修复 `/tenant/`、`/admin/` 下 `notifications`、`transWeb` 等请求落到站点根路径导致的 404。


### Changed

- **TransWebService 请求携带 auth token**: `initUpload`、`initDownload`、`downloadFile` 请求现在通过 `transClient` 拦截器自动携带 pinia auth token（`headers.token`），修复请求头缺失导致后端鉴权失败的问题
  - `initUpload`/`initDownload` 从裸 `axios.get` 改为 `transClient.get`，走拦截器
  - `transClient` 请求拦截器新增 auth token 注入（与 `rawClient` 保持一致）
  - `downloadFile`（fetch 方式）手动注入 `headers.token`
- **审批详情页数据展示修复**: 重构 `useApprovalDetail.ts`，改为直接使用 `ApplicationDetailResponse`（真实接口）作为数据源，修复基本信息/申请信息为空的问题
  - `detailData` 类型从 `Application`（mock）改为 `ApplicationDetailResponse`
  - `basicInfoRows`/`applicationInfoRows` 从 `appBaseInfo`/`appBaseApprovalRoute`/`appBaseCountryCityRegionRelation`/`appBaseUploadDownloadInfo` 提取真实字段
  - 移除对 `applicationStore` mock 数据的依赖
- **审批详情页时间线修复**: 审批时间线从 mock `ApprovalTimeline` 组件替换为真实 `ProcessTimeline` 组件，自动调用 `getProcessDetails` 接口获取流程进展
- **审批操作组件可见性修复**: `canOperate` 判断逻辑从基于 mock role 层级改为基于后端 `appBpmWorkFlow.currentHandler` 与当前用户 w3Account 匹配，修复审批人看不到审批操作组件的问题
  - `statusLabel`/`canOperate` 改为基于后端 `applicationStatus` 数字判断（2=待审批）
  - `ApprovalDetailView.vue` 中所有 `detailData?.xxx` 引用更新为 `detailData?.appBaseInfo?.xxx` 等真实字段路径
- **审批人判断逻辑修正**: 从列表接口 `/waitingForApproval` 获取 `currentHandler`，通过路由 query 传递到详情页，与当前登录用户 `username` 匹配来判断审批权限
  - `ApprovalListItem` 新增 `currentHandler` 字段
  - 列表跳转详情时通过 `query.currentHandler` 传递
- **审批操作接口对接真实后端**: 通过/驳回/免审统一调用 `POST /workflowService/services/frontendService/frontend/application/userApproved`，替换原 mock 接口
  - `approvedType: 1`（通过）/ `0`（驳回），`isSecurityLevelChange` 固定传 `"0"`
  - `handleApprove`/`handleReject`/`handleExempt` 直接调用 `approvalApi.userApproved`，不再通过 approvalStore
- **审批操作交互优化**: 审批成功后弹 `Modal.success` 对话框提示结果，点击"查看"跳转到"我已审批" tab；接口返回非 `code: "200"` 时提示"操作失败"
  - 列表页 `useApprovalList` 支持通过 `?tab=approved` query 初始化激活的 tab
- **审批列表"传输类型"列显示为空**: `TRANSFER_TYPE_LABEL_MAP` 缺少反向区域组合（如 `yellow-to-green`、`red-to-green` 等），已补全所有 9 种区域组合的类型定义和标签映射
- **已驳回单审批状态显示修正**: 后端 `currentStatus` 为"创建申请单"时，前端映射为"申请人重填"

### Changed
- **审批时间线增强**: `ProcessTimeline` 组件合并流程进展接口和审批日志接口数据，展示完整审批过程
  - 新增 `GET /approvalLog/page/999/1` 接口获取审批日志（`ApprovalLogItem` 类型定义）
  - 审批日志按名称（`statusName` ≈ `approve_node_name`）匹配，不匹配则按 `lastUpdateDate` 匹配
  - 匹配到的审批节点展示：通过/驳回标签、审批人 `approver_w3_account`、审批意见 `comments`
  - 驳回节点时间线圆点变为红色，图标使用 `IconCloseCircleFill`

### Added
- **文件下载功能对接**: "待我下载"和"审批管理"详情页支持文件下载，"我的申请单"详情不显示下载按钮
  - 新增 `useFileDownload` composable，封装下载流程（extractParams → initDownload → downloadAndSave）
  - 单文件下载：从 `downloadUrl` 提取 params → `initDownload` 获取 token → `downloadAndSave` 保存文件
  - 批量下载：串行逐个下载，首次初始化 token 后复用
  - `DetailFileTable.vue` 新增 `showDownload` prop，替代原 `status` 控制下载按钮显示
  - `ApplicationDetailView.vue` 读取 `route.query.showDownload` 决定是否显示下载按钮
  - `ApprovalDetailView.vue` 始终显示下载按钮（`:show-download="true"`）
  - `DownloadListView.vue` 跳转详情时携带 `?showDownload=true`
  - `useApprovalDetail.ts` 对接 `getApplicationDetail` 真实接口获取 `downloadUrl`
- **详情页文件列表真实接口对接**: 申请详情和审批详情页文件列表对接真实后端接口
  - 新增 `useFileList` 共享 composable，封装文件列表获取、分页、重置逻辑
  - 新增 `getMyFileInfoList` API 接口（`GET /myFileInfo/page/{pageSize}/{pageNum}?applicationId={id}`）
  - 新增 `MyFileInfoItem`/`MyFileInfoResponse` 类型定义
  - `DetailFileTable.vue` 新增分页支持，移除"上传时间"列（后端暂未提供该字段）
  - `ApplicationDetailView.vue` 和 `ApprovalDetailView.vue` 文件列表 tab 显示总数、支持分页切换
  - 复用 `RealPageVO` 类型作为分页数据结构
  - `region.ts`: 区域 ID 映射、标签映射、下拉选项、工具函数
  - `transferType.ts`: 传输类型标签映射、下拉选项、工具函数
  - `index.ts`: 统一导出入口
- **申请单流程进展组件**: 新增 `ProcessTimeline.vue` 组件，展示申请单的流程进度
  - 新增 `getProcessDetails` API 接口和 `ProcessDetailsResponse`/`ProcessStepItem` 类型
  - 组件支持自动获取流程数据，按时间排序展示步骤列表
  - 步骤状态判断：已完成（绿色）、进行中（蓝色）、待处理（灰色）
  - 用时格式化：秒 -> "X分X秒" 或 "X小时X分"
  - 样式参考 `ApprovalTimeline` 组件

### Changed
- **区域常量统一重构**: 解决霰弹式修改问题
  - 消除 3+ 处重复的 `REGION_TYPE_MAP` 定义
  - 消除 3+ 处重复的传输类型标签映射
  - `useApplicationForm.ts`、`useTransferConfig.ts`、`payloadConverter.ts` 等文件改用统一常量
  - 旧导入路径保持向后兼容（`@/views/application/components/constants` 重导出）
  - `SecurityArea` 和 `TransferType` 类型定义移至 `constants/` 目录，`types/application.ts` 改为重新导出，保持单一来源
- **SelectType 跳转参数优化**: 
  - 跳转时携带 `from` 和 `to` 参数，避免创建页重新推断区域
  - `useApplicationForm` 新增 `fromZone`、`toZone` 参数，优先使用传入值
- **申请单详情状态展示优化**:
  - status-tag 改用 `processDetailData.applicationStatus` 展示（如"流程结束"）
  - 移除原有的 `currentHandler` 展示
  - `useApplicationDetail.ts` 新增 `processDetailData` 和 `fetchProcessDetail` 方法
  - 详情页在信息卡片下方新增"流程进展"区块
- **创建页"继续上传文件"功能**:
  - 新增 `loadApplicationById` 方法，从后端详情接口加载已存在的申请单数据
  - 新增 `transWayToTransferType` 函数，从 transWay 字符串推断 transferType
  - 新增 `extractParamsFromUrl` 函数，从 uploadUrl 提取 params 参数
  - 新增 `REGION_ID_TO_AREA` 反向映射
  - `CreateApplicationView.vue` 支持 `applicationId` 参数，自动加载数据并进入第二步
  - 第一步表单数据回填并置灰（readonly）
- **详情页默认TAB调整**: 默认展示"申请单信息"而非"文件列表"
- **关闭申请单功能重构**:
  - 修复 `closeApplication` API 为 GET 请求，响应类型为 `boolean`
  - 新增 `CloseApplicationModal` 组件，支持手动输入申请单号二次确认
  - 详情页和列表页统一使用 `CloseApplicationModal` 组件
  - 从 `useApplicationDetail.ts` 移除 `handleCloseApplication` 方法
- **申请单详情对接真实接口**:
- **申请单详情对接真实接口**:
  - 新增 `ApplicationDetailResponse` 及相关类型定义（`AppBaseInfo`、`AppBaseApprovalRoute`、`AppBaseCountryCityRegionRelation`、`AppBaseUploadDownloadInfo` 等）
  - 新增 `applicationApi.getApplicationDetail` 方法，对接 `GET /workflowService/services/frontendService/frontend/application/approvalDetails`
  - 新增 `applicationApi.closeApplication` 方法，对接关闭申请单接口
  - 重构 `useApplicationDetail.ts`，改为调用真实接口
  - 更新详情页展示字段：申请人、申请单号、当前处理人、存储空间大小、上传/下载有效期间、部门、传输路由、上传/下载区域、源/目标城市、下载人账号、抄送人、客户网络数据、创建时间、申请原因、通知范围
  - 更新操作按钮：继续上传文件（isUploaded=0 时出现）、关闭申请单
  - 移除删除申请单、继续编辑按钮
- **我的申请单列表对接真实接口**:
- **我的申请单列表对接真实接口**:
  - 新增 `MyApplicationItem`、`MyApplicationResponse`、`MyApplicationQuery` 类型
  - 新增 `applicationApi.getMyApplicationList` 方法，对接 `POST /workflowService/services/frontendService/frontend/myApplication/page/{pageSize}/{pageNum}`
  - 重构 `useApplicationList.ts`，改为调用真实接口
  - 更新表格列：申请单号、传输路由、当前流程、申请单状态、申请原因、对方名称、创建时间、操作
  - 更新操作列：文件列表、复制申请、关闭申请单、查看详情
  - 移除原有的状态筛选、下载人、源城市、目标城市列
- **创建页离开确认交互重构**: 移除草稿保存能力，改为按步骤差异化提示
  - 第1步：无变更直接离开，有变更弹窗提示"离开后当前填写内容将丢失"
  - 第2步：无论有无变更都弹窗提示"离开后需要重新上传文件，可在我的申请单继续操作"
  - 第3步：直接离开无拦截
- **移除自动保存草稿**: 因真实后端无草稿能力，移除 `useApplicationForm.ts` 中每 30 秒自动保存草稿的逻辑

### Added
- **人员搜索选择器对接真实接口**: 新增 `UserSuggestSelect.vue` 通用组件，对接 `POST /workflowService/services/frontendService/frontend/suggestUser`
  - 新增 `src/api/auth.ts`：`suggestUser` 接口和 `SuggestUserItem`、`SuggestUserResponse` 类型
  - 新增 `src/composables/useUserSuggest.ts`：防抖搜索（300ms）、至少3字符触发、数据转换
  - 新增 `src/components/business/UserSuggestSelect.vue`：支持单选/多选模式
  - 替换 `StepOneBasicInfo.vue` 中的下载人账号、抄送人、直接主管选择器
  - 显示格式：`displayNameCn / userAccount / dept`（中文名/账号/部门）
- **二/三/四层审批人对接真实接口**: 对接 `POST /workflowService/services/frontendService/frontend/approvalInfo/getAllApprovers`
  - 新增 `src/api/application.ts`：`getAllApprovers` 接口和 `ApproverItem` 类型
  - 更新 `useApprovalRoute.ts`：并行调用 `findApprovalRoute` 和 `getAllApprovers`
  - 按 `approverTypeId` 解析审批人到对应层级（0=一层, 1=二层, 2=三层, 3=四层）
  - 自动选中第一个审批人
  - 二/三/四层审批人使用普通下拉框，直接主管保持搜索框

### Changed
- **DepartmentSelector 重构**: 从 tree-select + Mock 数据重构为弹窗形式，对接真实接口
  - 新增 `src/api/dept.ts`：定义 `deptApi.suggestDept`（模糊查询）和 `deptApi.searchDeptByAccount`（用户部门路径）
  - 弹窗内支持模糊搜索（300ms debounce），搜索结果点击后自动回填所有层级下拉
  - 初始化时自动调用用户部门接口，根据 `parentDept` 构建多级联动下拉
  - 底部「已选路径」实时预览，确认后回填触发器
  - `change` 事件新增 `deptCode` 字段（向后兼容）
  - `LOGIN_USER_TYPE` 常量从 `auth.ts` 导出，保证 usertype 来源唯一
- **vite.config.ts**: 新增 `/user-center` 代理，target `http://127.0.0.1:8087`
- **StepOneBasicInfo 组件重构**: 按照 Vue 最佳实践和 antfu 规范重构组件，将 581 行大文件拆分为多个模块：
  - `constants.ts`: 提取常量（`REGION_TYPE_MAP`、`HIGH_TO_LOW_PAIRS`、`AREA_OPTIONS`、`MOCK_APPROVER_OPTIONS`）
  - `types.ts`: 提取类型定义（`StepOneBasicInfoProps`、`StepOneBasicInfoEmits`、`ApprovalRouteConfig`、`CitySelectionState`）
  - `useSecurityLevel.ts`: 提取密级选择逻辑（API 调用、高密传低密判断、自动选中）
  - `useApprovalRoute.ts`: 提取审批层级逻辑（审批路由查询、审批人字段显示控制）
  - `useCitySelection.ts`: 提取城市选择逻辑（上传/下载城市联动、区域变化响应）
  - 主组件从 581 行精简至约 320 行，职责更加单一
- **类型导出**: 导出 `SecurityArea` 类型供 composables 使用

### Fixed
- **StepOne 部门选择绑定契约修复**: `StepOneBasicInfo.vue` 改为向 `DepartmentSelector` 传递 `departmentId` 作为 `modelValue`、`department` 作为展示值，消除 `deptCode` 与完整路径混绑问题。
- **DepartmentSelector 回显修复**: 新增 `displayValue` 受控回显支持，外部已有部门路径时可稳定显示触发器文本，并在清空/外部值切换时同步更新展示。
- **创建页递归更新修复**: 为 `StepOneBasicInfo.vue` 的 `updateFormData` 增加差异比对，避免在 `defineModel` 数据流下重复写回相同表单值；同时将 `useSecurityLevel.ts`、`useApprovalRoute.ts`、`useCitySelection.ts` 的监听改为稳定 getter 数组源，并为审批清空逻辑增加空值保护，修复 `<CreateApplicationView>` 的 `Maximum recursive updates exceeded`。

### Added
- **Token 定时刷新**: 使用 VueUse `useIntervalFn` 在 auth store 实现全局 token 定时刷新（15分钟间隔），登录成功自动启动、登出自动停止；新增 `isRefreshing`、`isTokenRefreshActive`、`refreshToken()`、`pauseRefresh()`、`resumeRefresh()` 状态与方法。
- **真实登录接口对接**: 修改 `authApi.login()` 调用真实后端 `/service/v1/userCenter/authentication/login`，参数暂写死；`vite.config.ts` 新增 `/service` 代理到 `http://127.0.0.1:8087`。
- **请求头适配**: 修改 `request.ts` 请求头从 `Authorization: Bearer` 改为 `token` 字段；适配真实登录响应格式（`data` 直接是 token 字符串）。
- **持久化机制修复**: 修复 pinia 插件与手动 localStorage 操作冲突问题，统一使用 pinia 插件持久化，配置自定义 serializer 与 `STORAGE_KEYS` 一致；移除 `persistAuthState()`、`initAuth()` 函数；修改 `request.ts` 从 pinia store 读取 token 而非直接操作 localStorage。
- **创建申请单接口对接**: 新增 `src/utils/payloadConverter.ts`（前端表单到后端接口请求体转换器）；新增 `applicationApi.createReal` 方法调用真实后端接口 `POST /workflowService/services/frontendService/frontend/application/create`；新增 `useApplicationForm.handleSubmitReal` 方法；`vite.config.ts` 新增 `/workflowService` 代理配置。
- **第一步提交申请单**: 新增 `isApplicationCreated` 状态标记申请单是否已创建；新增 `handleNextWithSubmit` 方法在第一步点击"下一步"时调用真实接口创建申请单。
- **真实接口 Mock 兜底**: 在 `src/mocks/handlers/application.ts` 中新增对 `/workflowService/services/frontendService/frontend/application/create` 的 mock 拦截，当后端调不通时自动返回 mock 数据。

### Changed
- **request 工具函数**: 新增 `request.raw()` 方法，用于发送跳过 baseURL 的请求，直接使用完整 URL 路径。

### Changed
- **创建申请流程调整**: 第一步点击"下一步"时调用 `createReal` 接口创建申请单，成功后进入第二步上传文件；返回"上一步"时表单变为只读不可编辑；移除"保存草稿"按钮；接口失败时停留在当前步骤允许修改后重试。
- **StepOneBasicInfo 组件**: 新增 `readonly` 属性支持，当 `readonly=true` 时禁用所有表单控件（部门选择器、区域选择、城市选择、下载人账号、抄送人、客户数据选项、SR单号、申请原因、通知选项）。
- **CreateApplicationView 组件**: 移除"保存草稿"按钮及相关逻辑；更新 `onClickNext` 方法支持第一步调用创建接口；传递 `readonly` 属性给 `StepOneBasicInfo`。

### Changed
- **创建申请单数据结构调整**: 移除废弃字段 `storageSize`、`uploadExpireTime`、`downloadExpireTime`、`customerAuthFile`；新增字段 `departmentId`、`sourceCityId`、`targetCityId`；更新 `types/application.ts`、`useApplicationForm.ts` 类型定义和默认值。
- **DepartmentSelector 组件**: 返回值改为 `{ deptId, deptName }` 格式，`deptName` 为完整路径格式如 `"研发部/平台研发组"`。
- **CitySelector 组件**: 返回值改为 `{ province, city, cityId }` 格式；`CityItem` 接口新增 `id` 字段。
- **StepOneBasicInfo 组件**: 移除废弃字段展示（存储空间大小、上传/下载有效期、客户授权文件）；适配新的 change 事件格式。
- **CreateApplicationView 组件**: 提交按钮改为调用 `handleSubmitReal` 使用真实接口；移除 `setCustomerAuthFile` 相关逻辑。

### Added
- **P11 TransWebService 文件上传下载**: 新增 `src/api/transWebService.ts`（完整类型定义、Token 管理、初始化/文件列表/分片上传/哈希校验/文件下载 API 封装）；新增 `src/composables/useTransUpload.ts`（初始化、分片上传、客户端 SHA-256 哈希计算 Mock、服务端哈希轮询校验、暂停/继续/取消、确认完成）；新增 `src/composables/useTransDownload.ts`（初始化、目录导航、文件/文件夹下载、打包状态检查、批量下载）；新增 `src/views/trans/TransUploadView.vue` + `trans-upload.scss`（拖拽上传区域、上传列表、哈希校验状态、已上传文件管理）；新增 `src/views/trans/TransDownloadView.vue` + `trans-download.scss`（文件列表、目录导航、批量选择下载、下载进度提示）；新增路由 `/trans/upload` 和 `/trans/download`（从外部跳转、通过 params 参数验证）。
- **传输类型配置动态化**: 新增传输页签配置 (`UITransferTabConfigItem`) 和传输类型配置 (`UITransferTypeConfigItem`)，支持动态配置 `/dashboard` 菜单下的 Tab（绿区传出、黄区传出...）和每个 Tab 下的传输类型卡片；界面配置页面新增"传输页签配置"和"传输类型配置"两个 Tab，支持完整的 CRUD、排序、状态切换；新增 `useTransferConfig` composable 供页面使用；`SelectTypeView.vue` 改为从配置 API 获取数据，移除硬编码数据。
- **P6.6.3 申请单配置动态化**: 新增 `application` 配置类型，扩展 `UIConfigTab`、`UIApplicationConfigItem` 类型；扩展 `uiConfigApi` 支持申请单配置 CRUD；扩展 `useUIConfig` composable；新增 `useApplicationConfig` composable 提供配置读取方法；界面配置页面新增"申请单配置" Tab，支持四种配置类型（申请人通知选项、下载人通知选项、最近传输模板、注意事项）的新增/编辑/删除/排序；`StepOneBasicInfo.vue` 改为从配置中读取动态数据。
- **P6.6.1 部门选择器**: 新增 `DepartmentSelector.vue` 与 `DepartmentSelector.scss`，使用 `a-tree-select` 替代原有的 `a-modal + a-tree` 方案，支持搜索、清空、v-model 双向绑定；替换 `StepOneBasicInfo.vue` 中的部门选择逻辑。
- **P6.6.2 城市选择器**: 新增 `CitySelector.vue` 与 `CitySelector.scss`，使用 `a-cascader` 展示省份-城市级联；更新 `cities.ts` 为省份-城市结构（10省份/24城市），新增 `DEFAULT_CITY` 默认城市常量；替换 `StepOneBasicInfo.vue` 城市选择器，标签改为"省份/城市"；`useApplicationForm.ts` 支持默认城市填充。
- **P7.2 文件分片上传单元测试**: 新增 `src/composables/__tests__/useFileUpload.spec.ts`（15 个测试用例全通过），覆盖 generateFileId、uploadFile（单/多分片、进度回调、IndexedDB 存储、错误处理）、断点续传（resumeUpload）、暂停/取消（pauseUpload/cancelUpload）、进度查询（getUploadProgress）、ArcoDesign 适配（customRequest）；使用 Mock 替代真实 IndexedDB（happy-dom 不支持 IndexedDB API），测试执行时间 199ms。
- **P10.9 界面配置**: 新增 `src/views/admin/UIConfigView.vue`、`ui-config.scss`，支持文字内容配置（树形编辑）、申请单卡片配置（新增/编辑/删除/顺序调整）、国际化配置（语言启停+翻译编辑）、按钮显隐配置（角色权限+条件 JSON+状态切换），并提供四类配置 JSON 导入/导出；新增 `src/composables/useUIConfig.ts`、`src/api/uiConfig.ts`、`src/mocks/handlers/uiConfig.ts`、`src/types/uiConfig.ts`；新增路由入口 `src/views/ui-config/index.vue` 并在 `src/router/routes.ts` 注册 `/ui-config`；更新 `api/index.ts`、`types/index.ts`、`mocks/handlers/index.ts`；新增 `useUIConfig.spec.ts`（12 个测试用例全通过）。
- **P10.8 传输通道管理**: 新增 `src/views/admin/ChannelManageView.vue`、`ChannelManageModal.vue`、`ChannelServerModal.vue` 与 `channel-manage.scss`，支持通道 CRUD、状态切换、服务器穿梭选择与拖拽排序；新增 `src/composables/useChannelManage.ts`、`src/api/channelManage.ts`、`src/mocks/handlers/channelManage.ts`、`src/types/channelManage.ts`；新增路由入口 `src/views/channel/index.vue` 并在 `src/router/routes.ts` 注册 `/channels`；更新 `api/index.ts`、`types/index.ts`、`mocks/handlers/index.ts`；新增 `useChannelManage.spec.ts`（12 个测试用例全通过）。
- **P10.7 区域管理**: 新增 `src/views/admin/RegionManageView.vue`（双 Tab：城市与安全域映射 / 可选安全域配置）、`src/views/admin/RegionManageModal.vue`（城市映射/安全域 共用弹窗，含表单校验与预设颜色选择器）、`src/composables/useRegionManage.ts`（城市/安全域完整 CRUD + 状态切换 + Tab 联动）、`src/api/regionManage.ts`、`src/mocks/handlers/regionManage.ts`（12条城市+4个安全域默认数据，全 CRUD Mock）、`region-manage.scss`；更新 `src/router/routes.ts` 新增 `/region` 路由，新增路由入口 `src/views/region/index.vue`；更新 `api/index.ts`、`mocks/handlers/index.ts`、`types/index.ts` 聚合导出；新增 `useRegionManage.spec.ts`（16 个测试用例全通过）。


 与样式 `src/views/download/download-list.scss`，基于 Figma `4088_301` 实现标题、筛选、列表、分页与操作区。
- **P6.7 下载逻辑**: 新增 `src/composables/useDownloadList.ts`，支持下载人可见性过滤、下载状态聚合（未下载/部分下载/已下载）、分页筛选与批量触发应用文件下载。
- **P6.7 单元测试**: 新增 `src/composables/__tests__/useDownloadList.spec.ts`，覆盖默认状态、可见性过滤、下载状态聚合与筛选重置。
- **单元测试**: 运行 `pnpm test:coverage`，14 个测试文件 53 个测试用例全部通过。
- **覆盖率报告**: 本次全量覆盖率执行通过（v8），并完成 `useDownloadList.spec.ts` 覆盖率校验。
- **工时统计模板**: 按 SSD 口径在 `docs/exec/*` 增加工时统计表字段（基线/AI工时/交互轮次/人工介入/返工/缺陷/覆盖率），并补齐缺失的 `task_P0/P1/P2/P3/P4/P7/P9/P10/P11` 模块级 exec 占位文件。
- **工时估算回填**: 基于“累计3天=24h”完成 P0~P11 第一版模块级估算回填，汇总口径更新为基线 45h、AI 24h、节省 21h、总节省率 46.7%。
- **P9.2 传输模拟逻辑**: 完善 `src/composables/useTransferSimulator.ts`，新增暂停/继续/取消与实时传输状态快照；在 `src/stores/file.ts` 集成传输状态、审批后自动开始传输及完成通知；补充 `useTransferSimulator.spec.ts`、`file.spec.ts`、`useApprovalDetail.spec.ts` 覆盖关键链路。
- **P9.1 传输进度组件**: 新增 `src/components/business/TransferProgress.vue` 与独立样式 `TransferProgress.scss`，支持状态标签、进度展示、暂停/继续/重试操作；并接入 `ApplicationDetailView.vue`、`ApprovalDetailView.vue` 展示传输进度，新增 `TransferProgress.spec.ts`，全量覆盖率更新为 16 个测试文件、65 个用例通过。
- **P9.3 传输管理页面**: 新增 `src/views/transfer/TransferManageView.vue`、`src/composables/useTransferManage.ts` 与 `transfer-manage.scss`，支持管理员查看传输任务、Tab/筛选/分页、单条暂停继续重试及批量操作；`src/views/transfers/index.vue` 由占位页切换为真实实现，新增 `useTransferManage.spec.ts`，全量覆盖率更新为 17 个测试文件、68 个用例通过。
- **P10.4 用户管理**: 新增 `src/views/admin/UserManageView.vue`、`src/views/admin/UserManageModal.vue`、`src/composables/useUserManage.ts` 与 `user-manage.scss`，支持管理员进行用户 CRUD 操作、角色分配、状态切换、重置密码；扩展 `src/api/user.ts` 与 `src/mocks/handlers/user.ts` 支持筛选参数（keyword/role/department/status）、删除用户、重置密码接口；新增 `useUserManage.spec.ts`，全量覆盖率更新为 19 个测试文件、83 个用例通过。
- **P10.5 系统配置**: 新增 `src/views/admin/SystemConfigView.vue`、`src/composables/useSystemConfig.ts`、`src/api/systemConfig.ts`、`src/mocks/handlers/systemConfig.ts` 与 `system-config.scss`，支持管理员配置传输参数（文件大小、分片、并发、有效期）、审批规则（11条层级映射、超时设置）、通知服务（邮件、短信、事件触发）、存储策略（有效期、清理周期）；更新 `src/views/settings/index.vue` 路由；新增 `useSystemConfig.spec.ts`（12个测试用例）。
- **P10 模块设计补充**: 在 `docs/tasks/task_P10.md` 补充三个缺失模块详设：P10.7 区域管理（城市-安全域映射、域配置）、P10.8 传输通道管理（加密配置、服务器管理）、P10.9 界面配置（文本配置、卡片配置、国际化、按钮显隐）。
- **P7，8，9 模块设计补充**:  完成AI 工时统计补充






- **测试修复**: 修复 `useApprovalDetail.spec.ts` 测试代码，补充 `transferTypeLabel` 返回值。
- **测试修复**: 修复 `guards.spec.ts` 避免加载含 SVG 引用的真实组件。
- **Bug 修复**: 修复申请详情页文件列表显示问题，`useApplicationDetail.ts` 现在从 `fileStore.getFilesByApplicationId()` 获取真实上传的文件。
- **Bug 修复**: 修复 `FileUpload.vue` 使用 Mock 上传的问题，现在使用真实的 `useFileUpload` 分片上传，并在上传成功后通过 `fileStore.addFile()` 将文件信息存入 Store。
- 完成 P6.1：新增选择传输类型页面 `src/views/application/SelectTypeView.vue`，按 Figma 3971_812 还原（Tab 分组 + 传输类型卡片 + 安全域渐变图标 + 审批层级标签 + hover 上浮效果）。
- 完成 P6.1：新增独立样式文件 `src/views/application/select-type.scss`，玻璃拟态卡片 + 响应式 3→2→1 列布局。
- Figma 资源同步：`public/figma/3971_812/`（19 个 SVG 文件）。
- 新增 `docs/diff/P6_Figma_Doc_Diff.md`：P6.1 Figma 与文档差异记录（7 项差异）。
- 新增 `docs/exec/task_P6_exec.md`：P6 执行记录。
- 完成 P6.4（首版）：新增申请单详情页 `src/views/application/ApplicationDetailView.vue` 与 `src/composables/useApplicationDetail.ts`，支持「申请单信息 / 文件列表」双 Tab、状态展示及按状态操作按钮。
- 完成 P6.4（抽象复用）：新增通用详情组件 `src/components/business/detail/DetailInfoSection.vue`、`DetailFileTable.vue` 及独立样式文件，供后续审批单详情复用。
- Figma 资源同步：`public/figma/3971_1904/`（9 个 SVG 文件）。
- 新增 P6.4 快速上手文档 `docs/guides/P6_ApplicationDetail_quickStart.md`：覆盖页面入口、复用组件、开发验收与 QA 回归路径。
- 完成 P8.1：新增待审批列表页面 `src/views/approvals/ApprovalListView.vue` + `src/composables/useApprovalList.ts` + `approval-list.scss`，实现 Tab 切换（待我审批/我已审批/全部审批）、筛选区域（申请单号、传输类型、申请人、申请时间）、数据表格（含审批层级标签）、分页功能。
- 完成 P8.2 & P8.4：新增审批详情页面 `src/views/approvals/ApprovalDetailView.vue` + `src/composables/useApprovalDetail.ts` + `approval-detail.scss`，实现审批时间线组件 `ApprovalTimeline.vue`、复用 `DetailInfoSection` / `DetailFileTable`、支持通过/驳回/免审操作。
- 完成 P8.3：实现审批操作完整逻辑，审批通过时判断是否为最后一级（根据 transferType），最后一级提示"已自动开始传输"，非最后一级提示"已通知下一级审批人"，操作成功后1.5秒自动跳转回审批列表。
- 新增 P8 快速上手文档 `docs/guides/P8_quickStart.md`：覆盖审批详情、审批操作、审批时间线。
- 完成 P7.1：新增通用文件上传组件 `src/components/business/FileUpload.vue` + `file-upload.scss`，支持拖拽上传、点击选择、文件列表展示（图标、名称、大小、进度、速度、状态）、文件操作（暂停/继续/重试/删除）、全局操作（全部暂停/继续/清空已完成）、并发控制（最多3个同时上传）。当前使用Mock上传逻辑，待P7.2完成后集成真实分片上传。


### Changed
- **P10 模块进度**: 更新 `docs/tasks/task_P10.md`、`docs/TaskList.md`、`docs/exec/task_P10_exec.md` 与 `docs/exec/task_project_progress_summary_2026-03-06.md`，同步 `P10.3` 完成状态，并记录 `P10.1 / P10.2` 暂缓、后续优先 `P10.4`。
- **头部通知刷新**: 更新 `src/components/common/AppHeader.vue`，在切换账号后自动拉取当前用户通知，确保头部未读角标同步刷新。
- P6.1 补齐：按 Figma 3830_3 补充绿区传出缺失的 2 个传输类型（绿区传到外网、绿区传到海思红区），绿区传出 Tab 现有 5 种类型。

- P6.1 补齐：图标资源统一切换到 `public/figma/3830_3/`，描述文案对齐 Figma（非研发到非研发/研发/外网/红区/海思红区）。
- P6.1 补齐：渐变色对齐 Figma 3830_3（黄区 `#F2B313→#EE7214`、红区 `#FF6900→#FB2C36`），新增外网渐变 `#2B7FFF→#00BBA7`、海思红区渐变 `#AD46FF→#F6339A`。
- P6.1 补齐：网格布局从 3 列调整为 5 列（≥1400px），新增 1400px 响应式断点。
- 更新 `src/router/routes.ts`：新增 `/application/select-type`（选择传输类型）和 `/application/create`（创建申请单）路由。
- 更新 `src/router/routes.ts`：新增 `/application/:id`（申请单详情）路由。
- 更新 `src/views/application/ApplicationListView.vue`：查看动作改为跳转申请单详情页。
- 更新 `src/router/routes.ts`：新增 `/approvals/:id`（审批详情）路由，更新 `/approvals` 路由指向 `ApprovalListView.vue`。
- 更新 `src/api/approval.ts`、`src/stores/approval.ts`、`src/mocks/handlers/approval.ts`：新增审批历史查询链路 `GET /approvals/:id/history`。
- 删除审批管理旧占位文件：`src/views/approvals/index.vue` 与 `approval-index.scss`，已被列表页替代。
- 更新 `docs/tasks/task_P8.md`：勾选 P8.1（待审批列表）、P8.2（审批详情）、P8.3（审批操作）、P8.4（审批时间线）已完成项，P8 模块全部完成。
- 更新 `src/composables/useApprovalDetail.ts`：新增 `isLastLevel` 方法判断审批层级，审批通过后根据是否最后一级展示不同提示信息，操作成功后1.5秒自动跳转回审批列表。
- 更新 `src/mocks/handlers/approval.ts`：实现审批层级判断逻辑，审批通过时若非最后一级则 `currentApprovalLevel + 1` 并保持 `pending_approval` 状态，最后一级则状态改为 `approved`。
- 更新 `docs/tasks/task_P7.md`：勾选 P7.1（文件上传组件）已完成项。


### Fixed
- 修复审批链路用户隔离与流转异常：`我的申请单` 改为仅显示当前提交人数据（`useApplicationList.ts`）；审批提交起始层级修正为一级（`useApplicationForm.ts`），避免二级审批单首审后直接结束；审批详情文件列表改为展示真实上传文件（`useApprovalDetail.ts`）；审批 Mock 接口新增并启用当前用户作用域（`pending/processed/all`）及审批权限校验（`mocks/handlers/approval.ts`），`我已审批/全部审批` 不再跨审批人串单。
- 修复“我的申请单”页首刷后删除/撤回弹框样式异常问题：在 `src/main.ts` 全局补充引入 `@arco-design/web-vue/es/modal/style/css.js` 与 `@arco-design/web-vue/es/message/style/css.js`，确保 `Modal.*` / `Message` 首次触发即可正确渲染。
- 修复 `src/components/business/__tests__/FileUpload.spec.ts` 未初始化 Pinia 导致 `pnpm test:coverage` 失败的问题，在 `beforeEach` 中补充 `setActivePinia(createPinia())`。



### Added

- 完成 P5.1：新增登录页面 `src/views/auth/LoginView.vue`，按 Figma 3971_1023 还原（居中卡片 + Logo + 渐变标题 + Arco 表单 + Demo 快速体验表格）。
- 完成 P5.1：新增独立样式文件 `src/views/auth/login.scss`，复用 mixin。
- 完成 P5.2：新增 `src/composables/useLogin.ts`（表单校验 / 登录 / 快速登录 / 记住我 / 角色跳转）。
- 完成 P5.3：新增 `src/components/common/RoleSwitcher.vue` 角色切换器组件（仅开发环境渲染，a-dropdown + 角色标签颜色区分）。
- 完成 P5.3：新增 `src/components/common/styles/role-switcher.scss` 独立样式。
- Figma 资源同步：`public/figma/3971_1023/`（1.svg / 2.svg / 3.svg）。

### Changed
- 更新 `src/router/routes.ts`：登录页组件指向 `auth/LoginView.vue`。
- 更新 `src/components/common/AppHeader.vue`：将旧的 Demo select 切换器替换为 `RoleSwitcher` 组件。
- 更新 `src/components/common/styles/app-header.scss`：移除旧 `.role-switcher` / `.role-switch` 样式。
- 重组 `docs/` 目录结构：新增 `design/`（设计文档+UML）、`diff/`（Figma/文档差异）、`exec/`（执行记录）、`guides/`（quickStart 指南）四个子目录，原有散落文件归档至对应目录。
- 新增 `docs/diff/P5_Figma_Doc_Diff.md`：P5 登录模块 Figma 与文档差异记录。
- 新增 `docs/exec/task_P5_exec.md`：P5 执行记录（进度/验收/产出清单）。

### Added
- 完成 P3：新增状态管理与接口层文件 `src/stores/auth.ts`、`application.ts`、`file.ts`、`approval.ts`、`notification.ts`、`src/stores/index.ts`。
- 完成 P3：新增业务 API 文件 `src/api/application.ts`、`approval.ts`、`notification.ts`，并更新 `src/api/index.ts` 聚合导出。
- 完成 P3：新增 Store 单元测试 `src/stores/__tests__/auth.spec.ts`、`application.spec.ts`、`file.spec.ts`、`approval.spec.ts`、`notification.spec.ts`。
- 新增 P3 快速上手文档 `docs/P3_Store_quickStart.md`，用于指导状态管理层接入与使用。
- 完成 P4：新增路由与守卫实现 `src/router/routes.ts`、`src/router/index.ts`、`src/router/guards.ts` 与守卫单测 `src/router/__tests__/guards.spec.ts`。
- 完成 P4：新增布局与公共组件 `src/layouts/BlankLayout.vue`、`src/layouts/DefaultLayout.vue`、`src/components/common/AppHeader.vue`、`AppSidebar.vue`、`PageContainer.vue`。
- 完成 P4：新增页面实现 `src/views/dashboard/index.vue`（Figma 还原）及 `login/applications/approvals/transfer/transfers/users/settings/logs/profile/notifications/error` 页面文件。
- 完成 P4：新增权限指令 `src/directives/permission.ts`，并在 `src/main.ts` 注册 `v-permission` 与 `v-role`。
- 新增 P4 文档与高保真差异记录 `docs/P4_Figma_Doc_Diff.md`。



### Changed
- 更新 `qtrans-frontend/src/main.ts`：注册 `pinia` 与 `pinia-plugin-persistedstate`。
- 更新 `docs/tasks/task_P3.md`：子任务勾选与完成标志更新为 `[√]`。
- 更新 P4 布局细节：`AppSidebar` 修复图标渲染并将折叠按钮下沉至侧栏底部，`AppHeader` 调整为 `Demo切换 -> 通知 -> 姓名下拉`，并将样式拆分为独立 `scss` 文件复用 mixin。
- 更新头部用户区样式：按 Figma `3971_1105` 重构右上用户名与下拉面板样式，并将 `PageContainer.vue` 样式拆分至独立 `scss` 文件并复用 mixin。
- 完成 P6.2：新增 `CreateApplicationView` 三步创建流程与 `useApplicationForm`（自动草稿、条件字段、离开确认、提交成功态），并按 Figma `3960_2183` 对齐成功页样式。
- 完成 P6.2 增量补齐（Figma `3879_3344` / `3883_4645`）：Step1 拆分“基本信息/申请信息”、基本信息只读化、补充抄送人与右侧“注意事项/最近传输选择（一键复制）”、并对齐发起申请高亮进度与表单样式。
- 完成 P6.2 二次补齐：接入 P2.2 部门树 Mock 到发起申请部门选择器；布局改为固定顶栏+固定侧栏+内容区独立滚动；申请页底部操作横栏改为固定吸底并修正背景填充色。
- 完成 P6.2 三次补齐：修复步骤条首步高亮命中（active/process 双选择器）；将部门选择改为点击弹窗树选择并回填；吸底操作栏通过 `width: 100%` + `box-sizing: border-box` 贴齐页面底部与左侧边缘。
- 完成 P6.2 Step2 重构（基于 Figma 3883_5466）：双表格展示「传输任务」与「已上传文件列表」，支持实时进度条动画、批量暂停/继续/删除、文件类型图标映射、Mock 上传模拟，样式对齐 Figma 表格卡片与渐变进度条。
- 完成 P6.2 组件拆分重构：将 `CreateApplicationView.vue`（768 行）拆分为三个独立子组件 `StepOneBasicInfo.vue`（基本信息+申请信息）、`StepTwoUploadFile.vue`（双表格上传）、`StepThreeSubmitSuccess.vue`（提交成功），主组件精简至 231 行，提升可维护性。
- 修复 Figma 3883_5466 图标渲染问题：从 `assets/CodeBubbyAssets/3883_5466/` 复制 47 个 SVG 文件到 `public/figma/3883_5466/`，确保所有按钮与文件类型图标正确显示。
- 修复 P6.2 Step1 样式回归：`CreateApplicationView.vue` 改为非 `scoped` 引入 `create-application.scss`，恢复拆分后子组件（`StepOneBasicInfo.vue`）的布局与表单样式。
- 修复 P6.2 Step1 细节样式：统一「基本信息/申请信息」标题字体、恢复申请信息选择类控件默认白底、底部操作栏按钮改为左对齐，并将「包含客户网络数据」从按钮单选改回普通单选以对齐 Figma。
- 修复 P6.2 Step1 样式继承链路：`CreateApplicationView.vue` 改为脚本显式引入 `create-application.scss`，并将样式中的 `:deep(...)` 批量替换为普通后代选择器，恢复标题、表单控件与步骤条样式命中。
- 修复 P6.2 Step1 表单状态回归：为国家城市级联选择器启用 `path-mode` 并规范化数组值，解决选择后仍触发“请选择源国家/城市”；同时恢复 radio/checkbox 的 checked 填充态与勾选态显示。
- 修复 P6.2 Step1 国家/城市校验残留问题：补充 `a-cascader` 返回值归一化逻辑（兼容 string/number/array），避免有效选择被错误置空后重复触发“请选择源国家/城市”。
- 修复 P6.2 Step2 按钮图标未渲染：`StepTwoUploadFile.vue` 显式引入 Arco Icon 组件并替换 `icon-*` 标签，恢复上传按钮与表格操作列按钮图标显示。
- 修复 P6.2 Step3 视觉问题：成功态图标改为 `3.svg + 4.svg` 叠加显示对号；步骤条 `current` 改为 `currentStep + 1` 并补齐 `finish/process/active` 高亮；成功页底部按钮样式对齐 Figma（圆角、字体、色值与宽度）。
- 完成 P6.3 申请单列表页面：新增 `ApplicationListView.vue` + `useApplicationList.ts` + `application-list.scss`，实现搜索/状态筛选/高级筛选（时间范围）/表格分页/状态标签/草稿编辑删除与待审批撤回，并接入 Figma `3961_3234` 资源（复制 33 个 SVG 到 `public/figma/3961_3234/`）。
- 新增 P6.3 快速上手文档 `docs/guides/P6_ApplicationList_quickStart.md`：覆盖入口路由、筛选/分页/导出/操作行为、代码结构与扩展点。
- 修复 P6.3 申请单列表页问题：增加历史草稿字段归一化（兼容 string/array）避免 `sourceCity.join` 报错；补充申请原因/下载人/城市字段的空值兜底显示；操作列由文字按钮改为 Figma 图标按钮并对齐样式。
- 修复 P6.3 操作列可用性：为查看/编辑/删除/撤回按钮补充悬浮提示（tooltip）；将高保真缺失或重复语义图标替换为 Arco Icon（`IconEye`/`IconEdit`/`IconDelete`/`IconUndo`），提升可辨识度。


















## [2026-03-05]

### Added
- 完成 P7.2 文件分片上传逻辑：新增 `useFileUpload` composable，实现基于 IndexedDB 的断点续传、暂停/继续/取消功能，支持 5MB 分片上传，集成 AbortController 请求控制和实时进度追踪。
- 完成 P7.3 断点续传实现：基于 IndexedDB 的分片记录机制，上传前自动检测已上传分片并跳过，恢复上传进度，支持暂停后继续、关闭浏览器后恢复上传。
- 完成 P7.4 文件列表组件：新增 `FileList.vue`，支持 upload/download/view 三种模式，展示文件名、大小、上传时间、状态，提供删除/下载/预览操作，底部统计文件总数和总大小。
- 新增文件上传 API (`src/api/file.ts`)：uploadChunk（分片上传）、mergeChunks（分片合并）、deleteFile（删除文件）、getFileList（文件列表）。
- 新增 MSW 文件上传 Mock Handlers (`src/mocks/handlers/file.ts`)：模拟分片上传、合并、删除和列表查询接口。

### Changed
- 更新 `src/api/index.ts`：导出 file API 模块。

## [2026-03-04]

### Added
- 完成 P2.1：新增 `qtrans-frontend/src/mocks/db.ts`（Dexie 数据库与 `fileChunks`/`fileMetas` 表）。
- 完成 P2.1：新增 `qtrans-frontend/src/composables/useFileChunk.ts`（分片计算、切片、SHA-256 哈希）。
- 完成 P2.2：新增 Mock 数据文件 `users.ts`、`departments.ts`、`cities.ts`、`applications.ts`、`index.ts`、`demo-init.ts`。
- 完成 P2.3：新增 MSW 启动与拦截实现 `src/mocks/browser.ts`、`src/mocks/handlers/*`。
- 完成 P2.3：生成 `qtrans-frontend/public/mockServiceWorker.js`。
- 完成 P2.4：新增 `qtrans-frontend/src/composables/useFileUpload.ts` 与 `useTransferSimulator.ts`。

### Changed
- 更新 `qtrans-frontend/src/main.ts`：开发环境按 `VITE_MOCK_ENABLED` 条件启动 MSW。
- 更新 `qtrans-frontend/tsconfig.app.json`：补充 `baseUrl` 与 `@/*` 路径别名。
- 更新 `qtrans-frontend/src/utils/request.ts`：修正响应拦截器返回类型以通过 TypeScript 构建。
- 更新 `qtrans-frontend/package.json`：记录 `msw.workerDirectory`。
- 更新 `docs/tasks/task_P2.md`：已执行子任务由 `[ ]` 勾选为 `[√]`。

### Fixed
- 修复 `src/mocks/handlers/application.ts`、`src/mocks/handlers/user.ts` 在 `strict + noUncheckedIndexedAccess` 下的类型错误。

## [2026-03-03]
### Added
- 完成 P1.1：新增类型定义文件 `src/types/common.ts`、`user.ts`、`application.ts`、`file.ts`、`approval.ts`、`notification.ts`、`index.ts`。
- 完成 P1.2：新增工具函数文件 `src/utils/format.ts`、`validate.ts`、`storage.ts`、`file.ts`、`index.ts` 及对应单测。
- 完成 P1.3：新增 `src/utils/constants.ts`，统一维护业务常量与字面量类型。
- 完成 P1.4：新增 Axios 请求封装 `src/utils/request.ts`，并在 `src/api/auth.ts` 完成首个业务接口接入。

### Fixed
- 修复 `src/utils/__tests__/format.spec.ts` 与 `src/utils/format.ts` 在文件大小格式断言上的不一致问题（`1 KB/1 MB`）。

## [2026-03-02]
### Added
- 完成 P0：初始化 `Vue3 + TypeScript + Vite` 项目骨架，并配置 `@` 路径别名与严格模式。
- 完成 P0：新增基础目录结构（`src/api`、`src/components`、`src/composables`、`src/mocks`、`src/router`、`src/stores`、`src/types`、`src/utils`、`src/views/*`）。
- 完成 P0：新增工程化能力（ESLint/Prettier/Husky/lint-staged）与环境变量文件（`.env.development`、`.env.production`、`src/env.d.ts`）。


